datasource db {
  provider = 'sqlite'
  url = env("DATABASE_URL")
}

plugin ts {
  provider = '@core/typescript'
  output = '../../studio/src/generated'
}

/// Agent model
model Agent {
  id String @id @default(cuid())
}
