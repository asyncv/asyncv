# asyncv

这是一个 AI 助手项目，目标是作为常驻后台的服务，提供两大核心能力：

- 连接消息机器人（消息平台连接器）
- 连接 LLM 提供器（抽象的 LLM provider 接口）
- 提供 MCP 调用接口
- 提供 Studio 页面用于对不同的 agent 做相关的配置

核心指令


```
asyncv start
```

```
asyncv studio
```

